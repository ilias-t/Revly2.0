<%= form_for [@user, @tile], html: { class: "form new-tile-message" } do |f| %>
   
   <%= f.text_field :message, autofocus: true, autocomplete: "off", placeholder: "What's your moment?" %>

  <%= f.hidden_field :color, autocomplete: "off" %>

  <%= f.submit "Post" %>
  
<% end %>